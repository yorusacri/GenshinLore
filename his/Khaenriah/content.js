        document.addEventListener('DOMContentLoaded', () => {
            // Markdown 内容会硬编码进此数组
            const MD_PARTS = [];
            // __MD_PARTS_START__
            MD_PARTS.push(String.raw`# 第?章：坎瑞亚·未有之**梦**

在无始无终的永恒里，

人类将度过

安然**无梦**的一生。

但在神明视线的死角下，

仍有人想要做**梦**。

人类有人类的底蕴，

我们不是滤清「神选者」后剩余的残渣。

从世界之外，

我们取得否定世界的力量。<sup>1</sup>


## 葬火

此时的第三降临者和仙灵的始祖被深渊拦截，六侏儒（深渊眷属）设计在仙灵始祖默许下（白之公主睡觉时）「杀死」了第三降临者（王子）。

## 赤月王朝<sup>2</sup>

·建立

钉子砸下后，无数古代文明毁灭，

一群不再信仰神的人们在须弥地底建立无神的国度坎瑞亚。其国民不只纯血的贵族，背弃了神的七国人民来到此处也会被接纳为国民。

坎瑞亚有着先进的科技与庞大的野心。王国护卫队成立后，队员透过名为「至真之术」的剑法守护宫廷与王国　

国花为因提瓦特，这种花在摘下之后就会变得十分坚硬，曾遍布坎瑞亚的宫廷　

（记事者之匣中提到沙尔芬德尼尔覆灭时，一群人建立了无神的国度，指的应该就是坎瑞亚。森林书任务中提到一位学者的名言「诸位，坎瑞亚数千年的历史在看着我们！」　可得知坎瑞亚有数千年的历史，刚好与沙尔芬德尼尔覆灭的时间吻合。

因此坎瑞亚的成立时间大概率是6000年前，葬火之战期间。）　

之后坎瑞亚发明了各种战争兵器，包括「耕地机」，并且前往各地搜集寒天之钉碎片。

如今的反主，六侏儒和反抗的难民建立了坎瑞亚。

·对赤月的信仰

> 「坎瑞亚诸贵族的首上啊，赤红的月影早已殒落于渊天，你的血脉也已终于偏盲」
> 
> 「统领我等的仁善之主啊，若你依然怀有对人子的怜悯，就请饮下这杯中之酒吧」

那是漆黑之日尚未遍照地底的久远时岁，古老尊荣的氏族统治着那广袤的王国。

迂执的祭司说服王座上昏聩的君王相信，高天之上的赤月之骸即是万象的主宰，

因为月光的色泽流淌在凡人的血肉之下，隐匿于渊底的漆黑，亦当由赤月而出。

既然如此，人之王便应以赤月之名自称。依循两界的光与焰，裁定无常的命运。

于是渴盼超越之人筑起无数恢宏的高塔，祷求早已逝去的赤月为他们带来拯救。

直至被斥为异端的星象学者们自虚假之天的倒影中窥见了世间一切命运的本源，

直至再也无法浇灭的质疑与愤怒如野火烧遍无梦的疆土，最终焚向月色的高宫…

·世界之外的孩子

这是一个特别古老时候的故事，相传那个时候家禽与野鸟还没有分开。那个时候，照亮地下王国的是一轮赤月，而不是后来的黑日。因为王国位置特殊，总有世界之外的事物漏入其中。王国的武备会消灭其中的灾祸，但是其他事物要怎么处理呢？比如说，或许来自某个毁灭世界的孩子？

贤人中的一位对王上进言：「诸贵族的首上啊，我曾从一个孩子那里听闻异界的故事：曾有海民相信神明来自大海。每次发现流落的海难者，他们都会用最高的礼遇相待。只因他们认为神明会假借海难者的形态巡查人间。」国王说：「我听不懂。按你的意思来吧。」

（当然啰，王国境内并不存在传统意义上的海洋。王国最早的建立者，他们曾经见过山岳的轮廓在太阳强烈的光辉下变得模糊的景象，也曾经见过月光落在海面的细微波澜上，就像许多的珍珠。到了故事发生的年代，能够在王下描述这些情景的只有外来者与少有的为公务离开王国而返回的人。海洋一词往往是星辰投影的空间的喻体。）

期待着自所谓的海洋之外而来到王国的神——或者说能够超越神的人，他们建立了收容这种孩童的机构。在那之后，王国当中的孤儿或者是来自外界的流浪儿也会被接纳。

·两界之火

年幼的裴伦埃里对人生最早的记忆，就是被大人们要求爬过漆黑的甬道。这些通道或许是寒冷时节里烧火排烟的管道，里面布满煤灰，而且一丝能让烟逃出、或者光漏入的缝隙都没有。裴伦埃里爬行时，偶尔还会在黑暗中摔落。好在这个甬道本身设计就是为了让孩童行过，就算是摔落也并不会很痛，也并没有惹人生厌的虫网。在最后裴伦埃里走到了尽头，但是出口却并未打开。他敲了敲门，只有大人们无情的声音在询问：「你死了吗？」如果已经死了，又怎么能回答呢？但是大人们并不喜欢这个回复。他们反复问着一样的问题，直到裴伦埃里大喊：「死了！」大人们继续问：「那你看到了吗？」或许是黑暗带来的恐惧加上饥饿与疲惫，裴伦埃里眼中看到了幻象。赤红的月亮高悬在漆黑的夜空里，它突然转了过来，原来是巨大、惊恐的眼瞳。大人们打开了门，抱住满身煤黑的裴伦埃里：「你已经行过壁中炉内的两界之火，现在在这里得到新生。」


·占星

王国中的人们不曾见过大海，当然也从未目睹过真正的太阳。但即便是在这暗无天日的地方，亦有人妄图窥探群星的秘密。不过他们并非仰望星空，而是在镜面虚假的倒影中寻觅真实。

或许是因为位置实在特殊，在王国境内所映出的星图中常有异象闪烁，占星学家们将之记录下来，称为「渴真之天象」，其意即为渴望真实。

「或许镜中所倒映的不过虚假的幻影，真相唯有穿透幻影才能到达。」

摸索着冗长的星表，久而久之，占星学家们自然也产生了这般的想法，只是那时的哲人们还不知道，他们已然窥见了永恒无限的魔天的一角。


### 魔神战争时期

·安吉莉卡

那一天，教养院中迎来了新的成员。她是一位美丽的异国少女，自称是高贵的公主。因为故国被贵金之神打败，贵为祭司之女的她不愿承认新神，所以才流浪到了王国。她说她的名字叫安吉莉卡<sup>3</sup>，意思是「仿若天之神使」（仙灵）。美丽的安吉莉卡说自己将来只会和王国当中最强大的骑士结为夫妻。裴伦埃里对此不屑一顾，可赫雷贝里却被安吉莉卡所迷住。安吉莉卡时常在王国的井户之海附近散步，在那里她对赫雷贝里讲述了外面世界的种种事物。担心挚友的裴伦埃里也会同行，因那猜忌生得比好奇心要早，对于安吉莉卡所说的一切他虽心存怀疑，但希望终有一天能得以一见。

在安吉莉卡到来之后，赫雷贝里仿佛变了一个人，他制造事端，寻衅与其他骑士决斗。他赢了所有的决斗，转头便向安吉莉卡夸耀自己的勇武。可安吉莉卡对赫雷贝里所有的功勋，都只有淡淡一笑。善跑者赢过乌龟又算什么功绩呢？

「与黑骏<sup>4</sup>相伴的尼约德，深秘院最擅长战斗的阿尔夫，一半骑士的统领亚尔伯里奇，不败的裴伦埃里。」安吉莉卡说出了她心目中，王国彼时的最强候选。

陷入热恋的赫雷贝里因此做出了他内心里再顺理成章不过的决定。他被称呼为叛徒，或者人们认为他已经疯狂。不过就算是这样，他也没有任何想要伤害裴伦埃里的意思。

而裴伦埃里，他坚信只要杀死了安吉莉卡，他挚友的疯癫（深渊力量的影响？）就可以得到治疗。

在最无情的追猎中，三人离开了国界。在那一瞬间，赫雷贝里捂住了自己的脸，口中的语言也逐渐像是野兽的嘶鸣。

女巫安吉莉卡解释：「赫雷贝里是抛弃自己的神明来到王国的族民后裔。这就是王国当中有顽固的纯血派贵族的原因。这就是背叛自己神明的代价。

「而你，裴伦埃里，你是漂流而来的人。所以你没有这种诅咒。虽然没有比肩一个世界的崇高，但是你也有自己的命运。

「而我，在神明死去之前我都未曾背叛过一秒，所以我也不会有这种诅咒。但是你真的看清我是谁了吗？」

太阳在此刻升起地平，裴伦埃里手中的剑因为彻夜战斗带来的疲惫而滑脱。他用手背遮起额头，第一次看向太阳。等他再看向安吉莉卡时，发觉她并非是璃月的美丽少女或者恶毒的女巫。

「我是从命运中挣脱的自由。这是赫雷贝里苦苦追寻，对你却唾手可得的东西。」

裴伦埃里的面前空无一物，只有广阔的大地。

已有人知晓了深罪的秘密，因此窥探星空也成为了禁忌的知识。

如今王国中仅有少数人享有逾越禁令的特权，以便为独眼的君王服务。

 

然而凡事总有意外，当无知的学僧拭去遮蔽焚真之镜的浮尘，

那环绕着深邃视界的奇境天光，便再次照亮了狭小的观星室。

漆黑的乐园是何等的美丽，何等的璀璨，又是何等的…亵渎。

仅是一瞥，便已占据了年轻的拉乌斯万格全部的心智与渴望。

而这便是日后高颂天外渊薮的妖僧，一切故事与罪行的开端…


## 黑日王朝

·黑日取代赤月

有人发现了直接接触深渊的方法，于是发动政变，驱逐了赤月王的王室血脉，建立黑日王朝。


### 王国四柱

指的是坎瑞亚的四个支柱产业（炼金，军队，机械，占星）

|组织|人物|介绍|
|---|---|---|
|王室|「黑王」<br/>伊尔明苏尔|独眼的王者|
|骑士|「摄政元帅」<br/>安弗塔斯·亚尔伯里奇|驾驶遗迹巨像驰援须弥的白鹄骑士的统帅<br/>亚尔伯里奇族在独眼王者失能后担任摄政|
|<br/>|克罗达尔·亚尔伯里奇|创立深渊教团|
|<br/>|「末光之剑」<br/>戴因斯雷布|宫廷卫队成员<br/>未能阻止坎瑞亚灭国，因不死诅咒而流浪|
|<br/>|哈夫丹|戴因的战友|
|<br/>|「天柱骑士」<br/>瑟雷恩|后来加入愚人众，成为卡皮塔诺|
|<br/>|古瑟雷德|瑟雷恩的副官|
|教养院|尼约德|与黑骏相伴|
|<br/>|「妙音女」<br/>「天王奴」<br/>安吉莉卡|来自璃月的少女|
|深秘院|海洛塔帝|院长，首席御前法师|
|<br/>|艾弗提尔维特|副院长，宫廷法师，后来加入愚人众，成为皮耶罗|
|<br/>|阿尔夫|最擅长战斗|
|<br/>|「妖僧」<br/>拉乌斯万格|<br />|


### 坎瑞亚的五大罪人

|罪人|权能|关系户|介绍|
|---|---|---|---|
|「贤者」<br/>海洛塔帝|理|<br />|曾是深秘院院长和首席御前法师|
|「预言家」<br/>维瑟弗尼尔|时|戴因斯雷布（弟弟）|<br />|
|「黄金」<br/>莱茵多特|生|造物：阿贝多、巴窟纳瓦、厄里那斯、魔龙杜林|吞噬纳贝里士并成为生之执政<br/>「树为最完美的果实骄傲，它是如此圆满而无瑕疵。但是寄宿了梦的种子啊，不要止当果壳中的宇宙之王。」<sup>5</sup>|
|「极恶骑」<br/>苏尔特洛奇|空|丝柯克（徒弟）|曾是宫廷卫队成员|
|「猎月人」<br/>雷利尔|死|狂猎（造物）|曾是猎杀赤月遗民的特务|


·苏尔特洛奇

苏尔特洛奇是死囚的儿子，整日和一帮不良青年混迹街头，受过不少批评。虽然他受过不少苦难，但他始终保持乐观。他结识了身患重病的坎瑞亚贵族继承人斯克弗努格。苏尔特洛奇承认了斯克弗努格的天赋，同他一同切磋，二人都很快乐。十岁那年，他偷来了禁忌的研究资料，治好了斯克弗努格的病。

后来，苏尔特洛奇进入骑士学院，和戴因斯雷布成为最好的朋友，在哈登手下学习黑蛇剑术。哈登打压新一辈的骑士，二人都因为践行骑士精神而受到体罚，苏尔特洛奇更是失去了一条腿和一只手。后来他不断奋斗，成为坎瑞亚精英骑士之一。


### 深秘院

·蛇之七变

「蛇之七变」乃是极恶骑教授丝柯克的小技法。

它最初是坎瑞亚深秘院开发出来的小法术，本来的名字是「混沌渗透」，用来嘲弄大地上的元素力使用者。术士们可以用深渊力量诱发出类似元素力的变化。其原理类似于用深渊渗透现实，制造特定性质的负压，来诱发元素力的流入。

坎瑞亚本就以此为幻戏，只在表演与游行里使用。


·禁忌知识

已有人知晓了深罪的秘密，因此窥探星空也成为了禁忌的知识。

如今王国中仅有少数人享有逾越禁令的特权，以便为独眼的君王服务。

 

然而凡事总有意外，当无知的学僧拭去遮蔽焚真之镜的浮尘，那环绕着深邃视界的奇境天光，便再次照亮了狭小的观星室。漆黑的乐园是何等的美丽，何等的璀璨，又是何等的…亵渎。仅是一瞥，便已占据了年轻的拉乌斯万格全部的心智与渴望。

而这便是日后高颂天外渊薮的妖僧，一切故事与罪行的开端…


·拉乌斯万格被放逐

逾越身份的约束，已属蔑视君王的大恶，窥探深黑智慧，这般罪行更是绝不容赦。

然而拉乌斯万格对此也未曾辩驳，因他的心中除那光芒外已别无他物。很快来自裁判庭的判决降下，学者被逐出了深秘院，并判以流放之刑。

当拉乌斯万格终于走出洞穴，沐浴于真正的阳光下，同行之人发出痛苦哀嚎，唯独他身上却是什么也未发生。

那一刻，仿若穹境中的伟大存在就在他的面前示现。它亲自从芸芸众生中拣选了他，赐予他黑暗的恩典。如此幸运，又如此可悲，他最终知晓了自己的使命。

> 「火焰照亮了世界，也将其烧却。」
> 
> 「而我的命运，便正如飞蛾扑火。」

### 教养院

·赤月的贤人期待能从世界之外找到超越神明的存在。而教养院负责接收世界之外的孩子，并为其洗练，以便于控制。

·虽然直到赤月降下，黑日升起，再到黑日垂暮，王国的教养院都一直没能等到超越之人到来，但教养院中走出了许多奇人异士，他们中很多人都成为了王国伟大的骑士。裴伦埃里自是他那个时代无愧的首席，除非硬要让他和他的挚友赫雷贝里相较。他们本应该争夺功勋，通过荣耀的数量与大小，还有庆功宴的饮下的美酒之数来分出高下。但是不知道为什么，他们却走上了以命相搏的末路。

·戴因斯雷布

在伟大的王上宣布新王储继位时，他远远瞧见了那一缕金发。欢庆、喧闹的气氛与他并没有什么关系，抛洒的真花与假花最后还需要他协助清扫。他从后望里回过头，继续维持着宫门前广场的秩序。他目前还只是个普通的青年骑士罢了。

金发的王储则是疑惑地看了眼前的人山人海。他/她之前暂居宫中，此刻才知晓这个地下王国居然生活着这么多人。他/她本是失落的荣光之国孑遗，肩负着一个世界的责任。但在短暂的生活 里，他/她感受到了此处住民的善意，俯身担起大地的柱石，从世界之王成为了仅仅一国的希望。

青年的骑士出身虽然谈不上微末，然面对大势也可说得上不值得一提；金发的王储本身命运更加高贵，如今却失去了世界，独独眷恋一国。

后来，青年骑士得以晋升，被授予了「末光之剑」的称号；金发的王储则被末代的黑王领着，一步一步走下了王国的最深处。本来只是为了拯救因献上预言而被关入大狱、刺瞎双眼的哥哥，青年骑士召集了时代的麟儿们。他们救国的义举只是顺势而为。金发的王储却被用做无限吸收深渊力量的容器，成为了差点毁灭世界的钥匙。

这是崇高之人从光明跌落向深渊的途中，与微末之人从黑暗里攀登向上的轨道——它们交错的瞬间。

弑君、大义、杀心、背叛，直到一切成为一片荒芜。这之后，同病相怜的二人一同旅行的时间，相比两人过于漫长而且渐行渐远的人生，也几乎只是交错的一点罢了。


## 500年前 黑日王朝覆灭

·伊尔明和王储开始吸收深渊之力

·解救维瑟弗尼尔

为了拯救因献上预言而被下狱的哥哥维瑟弗尼尔，戴因召集了苏尔特洛奇、海洛塔帝、莱茵多特、雷利尔。几人发现了正在利用深渊力量的伊尔明和被用作无限吸收深渊力量的王储，于是制止了黑王，救下了他和维瑟弗尼尔。

·瓜分深渊力量，引发漆黑的灾祸

然而除了戴因之外的五人却受到深渊的蛊惑，瓜分了王储身上的深渊力量，成为了与世界匹敌的超然存在——五罪人。

雷利尔没有挽回索琳蒂丝，一怒之下接触深渊，成为罪人。<sup>6</sup>

被称为「黄金」的炼金士堕落为了罪人，孕育了大量漆黑的魔兽。

艾弗提尔维特试图阻止海洛塔帝，但失败。

面对几人造成的漆黑灾厄，苏尔特洛奇因为提瓦特已没有他感兴趣的东西，而离开了提瓦特。

·天理势力前往坎瑞亚

除了守护世界树的大慈树王，其余六神（温迪、钟离、雷电真、厄歌莉娅、玛薇卡、白沙皇）均被召集前往坎瑞亚抗击深渊。

|七神|战斗结果|
|---|---|
|温迪|存活，是现存的一代神|
|钟离|存活，是现存的一代神。与冰之女皇签订了终结一切契约的契约|
|雷电真|在坎瑞亚战死。接任的雷电影开始维系永恒，闭关锁国，颁布眼狩令|
|大慈树王|因感染禁忌知识，所以将自身从世界树抹除，最终被世界遗忘。作为世界树枝芽的纳西妲诞生，取代了大慈树王在世界上的所有记录|
|厄歌莉娅|在甘露花海战死，树王种下大树，让厄歌莉娅的意识得以寄宿。接任的芙卡洛斯为了应对预言，承受水神创造枫丹人的罪孽，开始计划摧毁水神神座|
|玛薇卡|将自身融入圣火，支援500年后对深渊的决战|
|白沙皇|战死。接任的冰之女皇性情大变，成为不再爱人的神，创立了愚人众，并开始收集神之心|

·覆灭

若娜瓦前往坎瑞亚，给纯血坎瑞亚人降下不死诅咒。而其他血脉的国民遭遇荒野诅咒而变成丘丘人。艾弗提尔维特见证了她降下诅咒的开端。<sup>7</sup>

黑日王朝最终覆灭，灾难突破了古国城墙，在大陆上散布开来。

> 「古国黑日落，明珠失其光。「黄金失其色，白绸染昏黄。」

是那所谓的报偿终究未曾到来，黑日殒落于同样的愚昧与傲慢。覆灭再临之时，唯有嗤笑的月光，洒落在早已灼成残灰的日影上。

> 「命运，命运，可怖而苍白的命运，你何竟卑屈于凶恣的僭君」
> 
> 「若是厄月之骸早已将你锚定于死，旧日血仇又有何意义可寻」
> 
> 「若是她编织的命运这般讥嘲我等，我等亦不妨高声讥嘲命运」
> 
> 「直至烬日最后的残影焚灭旧世界，直至赤月见证无垢的明晨」


·摄政

伊尔明失能。摄政元帅安弗塔斯代理摄政，领导救国会议，重启超级大宝，携带贫民出逃。　 

·克罗达尔创立深渊教团，并在蒙德制造污秽逆位神像

·命运的织机

「将敌人的信仰作为薪柴，为高贵的公主殿下献上荣光」

 改造奥塞尔，以倒挂神像（捧着第一座耕地机的眼睛）为基底，动摇神座，改写历史

·深境螺旋

由早已覆灭的远古文明，修建的巨大地下古都。究竟是出于何种目的而修建，如今已经不可知。其结构被分为连结世界的「深境回廊」与通往未知的「渊月螺旋」，故此统称为「深境螺旋」。据说，其中埋葬着早已被这个世界遗忘的秘密与宝藏…

相传通往天空岛之人，在神之眼中——在幻视中，看到的天梯也是如此螺旋的形态。我们走向宇宙，或者下到深渊。并没有差别，因为都是未知的漆黑。


·艾弗提尔维特前往至冬，成为「愚人」，效忠冰之女皇。

贤者自以为无所不知，我等才明白愚行背后的道义。既然沾染的同胞的血无法洗净，那索性成为嘲笑命运的「丑角」。既然我的才学无法与「贤者」比拟，没能获得上一位王者的青睐，也未能阻止他们撕开深罪的纱笼，招来神怒、毁灭与痴愚的狂潮，那就索性成为笨拙的「愚人」，为理解我的伤痛的「陛下」效忠…


### *旅行者兄妹降临全流程
· 

> 原初逆转了毁灭，天空之岛焚烧了大地之国。白垩追逐着黄金，赤月对黑日复仇。未来拯救过去，年长者与年幼者同血相残。——这是世间回环的命运。戴因，对你来说，这缕金发又是什么呢？必须杀死的人，还是忏悔的对象？
> 
> ——自称通晓命运之人（维瑟弗尼尔）

·兄妹降临坎瑞亚。主角和反主昏迷

·反主先醒，失忆，被坎瑞亚培养，视它为第二故乡

·反主记忆逐渐恢复，然后坎瑞亚陷落灾难爆发

·蒂莱尔在去寻找救世主的路上被时间执政伊斯塔露的力量卷入，与500年后的主角所在的时空重叠，遇到了主角，

> 情况一：主角给了钥匙。
> 
> 情况二：主角没给钥匙，但在那个重叠时空里，反主希望和主角在500年前见上一面，所以他一定会保证这个闭环的发生。如果主角不给钥匙，那反主就会把他的钥匙给出去。

·反主碰到了蒂莱尔以及她带来的飞船钥匙，没有拿走蒂莱尔手里来自未来的钥匙（如果是情况一，就是未来主角的；如果是情况二，就是未来反主的） 。蒂莱尔在收到不死诅咒变成丘丘人之后回到了山洞里等主角

·反主选择去查看主角情况，放弃了坎瑞亚人民。反主发现主角没事，钥匙还在。

·反主带着主角逃跑，被自称「天理维系者」的家伙拦截

·反主心有愧疚，决定用命运的织机复国坎瑞亚

·后接主线，反主利用夜神之国和教团记忆备份坎瑞亚

·主角找到了这把「蒂莱尔手里的钥匙」

（未完待续）

> 1. 《原神·提瓦特篇》主线章节预告PV-「足迹」_哔哩哔哩_bilibili
> 2. 赤月之形，裴伦埃里
> 3. 但是古时璃月的人并不会这样取名，如今想来，她名字或许是「妙音女」或者「天王奴」。
> 4. 初版作黑狼。从坎瑞亚考古中得知，应该是坎瑞亚炼金术制造的战兽其中一类。赤月王朝时期，炼金术与驯兽骑士乃是中流砥柱。但是在黑日王朝时期，因机械工学兴起而衰落了。
> 5. 阿贝多魔女的课业中，莱茵多特给阿贝多的寄语
> 6. 《原神》剧情PV-「月的倒影」_原神
> 7. 《原神·提瓦特篇》幕间PV-「神的局限性」_原神  05:09
`);
            // __MD_PARTS_END__
           const md = MD_PARTS.join('\n');

            const pageTitleEl = document.getElementById('page-title');
            const introEl = document.getElementById('timeline-intro');
            const timelineEl = document.getElementById('timeline');
            const tocList = document.getElementById('toc-list');

            const modal = document.getElementById('modal');
            const modalBody = document.getElementById('modal-body');
            const closeModal = document.getElementById('close-modal');

            function openModal(html) {
                modalBody.innerHTML = html;
                modal.style.display = 'flex';
                setTimeout(() => modal.classList.add('modal-active'), 10);
            }

            function closeModalFunc() {
                modal.classList.remove('modal-active');
                setTimeout(() => { modal.style.display = 'none'; }, 300);
            }

            closeModal.addEventListener('click', closeModalFunc);
            modal.addEventListener('click', (e) => { if (e.target === modal) closeModalFunc(); });
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && modal.style.display === 'flex') closeModalFunc();
            });

            function escapeHtml(text) {
                return text
                    .replaceAll('&', '&amp;')
                    .replaceAll('<', '&lt;')
                    .replaceAll('>', '&gt;')
                    .replaceAll('"', '&quot;')
                    .replaceAll("'", '&#39;');
            }

            function normalizeInline(raw) {
                let s = raw ?? '';
                // 保护允许的内联标签：<br> / <br/> / <sup>n</sup> / <sub>n</sub>
                s = s.replaceAll(/\r/g, '');
                s = s.replaceAll(/<br\s*\/?>/gi, '[[[BR]]]');
                s = s.replaceAll(/<sup>\s*(\d+)\s*<\/sup>/gi, '[[[SUP:$1]]]');
                s = s.replaceAll(/<sub>\s*(\d+)\s*<\/sub>/gi, '[[[SUB:$1]]]');
                // 保护 [Image](...) 格式
                s = s.replaceAll(/\[Image\]\s*\(\s*["']([^"']+)["']\s*\)/gi, '[[[IMG:$1]]]');
                s = escapeHtml(s);

                // markdown 加粗/斜体（轻量支持）
                s = s.replaceAll(/\*\*([^*]+?)\*\*/g, '<span style="color:red;">$1</span>');
                s = s.replaceAll(/(^|[^*])\*([^*]+?)\*(?!\*)/g, '$1<em>$2</em>');

                // 还原允许标签
                s = s
                    .replaceAll('[[[BR]]]', '<br/>')
                    .replaceAll(/\[\[\[SUP:(\d+)\]\]\]/g, '<sup>$1</sup>')
                    .replaceAll(/\[\[\[SUB:(\d+)\]\]\]/g, '<sub>$1</sub>')
                    .replaceAll(/\[\[\[IMG:([^\]]+)\]\]\]/g, (match, src) => {
                        // 将反斜杠替换为正斜杠，并将 \.. 转换为 ..
                        let fixedSrc = src.replaceAll('\\', '/').replace(/^\/?\.\.\//, '../../');
                        return `<img src="${fixedSrc}" alt="image" class="table-inline-image" style="display:block;margin:0 auto 8px;max-width:100%;height:auto;">`;
                    });

                return s;
            }

            function parseFootnotes(lines) {
                /** @type {Record<string, string>} */
                const map = {};
                let currentKey = null;

                // 外部链接映射（仅包含外部链接的脚注）
                const linkMap = {
                '1': '<a href="https://www.bilibili.com/video/BV1At4y1q7UQ/?spm_id_from=333.1387.upload.video_card.click&amp;vd_source=5521f8f34a1af08d13b6fcf39bf4506b" target="_blank">《原神·提瓦特篇》主线章节预告PV-「足迹」_哔哩哔哩_bilibili</a>',
                '6': '<a href="https://www.bilibili.com/video/BV1GPymBwE9K/?spm_id_from=333.1387.upload.video_card.click&amp;vd_source=5521f8f34a1af08d13b6fcf39bf4506b" target="_blank">《原神》剧情PV-「月的倒影」_原神</a>',
                '7': '<a href="https://www.bilibili.com/video/BV18tGHzoEyW/?spm_id_from=333.337.search-card.all.click&amp;vd_source=5521f8f34a1af08d13b6fcf39bf4506b" target="_blank">《原神·提瓦特篇》幕间PV-「神的局限性」_原神</a>'
                };

                for (const raw of lines) {
                    const line = (raw || '').trim();
                    if (!line.startsWith('>')) continue;

                    const m = line.match(/^>\s*(\d+)\.\s*(.*)$/);
                    if (m) {
                        currentKey = m[1];
                        const num = currentKey;
                        // 如果该脚注有外部链接，使用链接映射；否则使用原始内容
                        if (linkMap[num]) {
                            map[num] = linkMap[num];
                        } else {
                            map[num] = normalizeInline(m[2].trim());
                        }
                        continue;
                    }

                    // 续行
                    const cont = line.replace(/^>\s*/, '').trim();
                    if (currentKey && cont && !linkMap[currentKey]) {
                        map[currentKey] = (map[currentKey] || '') + '<br/>' + normalizeInline(cont);
                    }
                }

                return map;
            }

            function injectFootnoteTooltips(html, footnoteMap) {
                const replacer = (_, n) => {
                    const key = String(n);
                    const tip = footnoteMap[key] || '';
                    return `<p class="has-footnote"><sup>${key}</sup><span class="tooltip">${tip}</span></p>`;
                };

                return html
                    .replaceAll(/<sup>\s*(\d+)\s*<\/sup>/g, replacer)
                    .replaceAll(/<sub>\s*(\d+)\s*<\/sub>/g, replacer);
            }

            function splitRow(rowLine) {
                const trimmed = rowLine.trim();
                const core = trimmed.replace(/^\|/, '').replace(/\|$/, '');
                return core.split('|').map(c => (c ?? '').trim());
            }

            function isBlankCell(cell) {
                const t = (cell ?? '').trim();
                return t === '' || t === '<br/>' || t === '<br>' || t === '&nbsp;' || t === '&#160;' || t === '—';
            }

            function renderTable(blockLines, footnoteMap) {
                if (blockLines.length < 1) return null;

                const headerCells = splitRow(blockLines[0]);
                let bodyStart = 1;
                
                // 识别分隔行（包含---的行）
                const hasSeparatorRow = blockLines.length > 1 && blockLines[1].includes('---');
                if (hasSeparatorRow) bodyStart = 2;

                const bodyLines = blockLines.slice(bodyStart);
                const bodyCells = bodyLines.map(splitRow);
                const colCount = Math.max(headerCells.length, ...bodyCells.map(r => r.length));

                // 补齐列数
                while (headerCells.length < colCount) headerCells.push('');
                for (const r of bodyCells) while (r.length < colCount) r.push('');

                // 竖向合并：第 1 列空白视作沿用上一行
                const rowspan = new Array(bodyCells.length).fill(1);
                const skipFirstCol = new Array(bodyCells.length).fill(false);
                let lastNonEmptyRow = -1;
                for (let r = 0; r < bodyCells.length; r++) {
                    const first = bodyCells[r][0];
                    if (!isBlankCell(first)) {
                        lastNonEmptyRow = r;
                    } else if (lastNonEmptyRow >= 0) {
                        rowspan[lastNonEmptyRow] += 1;
                        skipFirstCol[r] = true;
                    }
                }

                // 横向合并：行内尾部空白并入左侧单元格
                /** @type {Array<Array<{text:string, colspan:number, skip:boolean}>>} */
                const mergedBody = bodyCells.map(row => row.map(text => ({ text, colspan: 1, skip: false })));
                for (let r = 0; r < mergedBody.length; r++) {
                    for (let c = 1; c < colCount; c++) {
                        if (isBlankCell(mergedBody[r][c].text)) {
                            // 找左侧最近的非空格
                            for (let k = c - 1; k >= 0; k--) {
                                if (!mergedBody[r][k].skip && !isBlankCell(mergedBody[r][k].text)) {
                                    mergedBody[r][k].colspan += 1;
                                    mergedBody[r][c].skip = true;
                                    break;
                                }
                            }
                        }
                    }
                }

                const table = document.createElement('table');
                table.className = 'lightrelam-table';

                // 检查表头是否有内容
                const hasHeaderContent = headerCells.some(cell => !isBlankCell(cell));

                // 渲染表头（仅当表头有内容时）
                if (hasHeaderContent) {
                    const thead = document.createElement('thead');
                    const headerRow = document.createElement('tr');
                    for (let c = 0; c < colCount; c++) {
                        const th = document.createElement('th');
                        let html = normalizeInline(headerCells[c] || '');
                        html = injectFootnoteTooltips(html, footnoteMap);
                        th.innerHTML = html || '';
                        headerRow.appendChild(th);
                    }
                    thead.appendChild(headerRow);
                    table.appendChild(thead);
                }

                // 渲染表体
                const tbody = document.createElement('tbody');
                for (let r = 0; r < mergedBody.length; r++) {
                    const tr = document.createElement('tr');
                    for (let c = 0; c < colCount; c++) {
                        if (c === 0 && skipFirstCol[r]) continue;
                        if (mergedBody[r][c].skip) continue;

                        const td = document.createElement('td');
                        if (c === 1 || c === 3) td.classList.add('text-left');
                        const cs = mergedBody[r][c].colspan;
                        if (cs > 1) td.colSpan = cs;
                        if (c === 0 && rowspan[r] > 1) td.rowSpan = rowspan[r];

                        let html = normalizeInline(mergedBody[r][c].text);
                        html = injectFootnoteTooltips(html, footnoteMap);
                        td.innerHTML = html || '';
                        tr.appendChild(td);
                    }
                    tbody.appendChild(tr);
                }
                table.appendChild(tbody);

                const wrapper = document.createElement('div');
                wrapper.className = 'table-container';
                wrapper.appendChild(table);
                return wrapper;
            }

            function parseMarkdownToPage(markdown) {
                const allLines = markdown.replaceAll('\r\n', '\n').replaceAll('\r', '\n').split('\n');

                // 识别文末注释区（以 > 数字. 开始的行作为起点）
                let footStart = -1;
                for (let idx = 0; idx < allLines.length; idx++) {
                    if (/^>\s*\d+\.\s+/.test((allLines[idx] || '').trim())) {
                        footStart = idx;
                        break;
                    }
                }

                const contentLines = footStart === -1 ? allLines : allLines.slice(0, footStart);
                const footnoteLines = footStart === -1 ? [] : allLines.slice(footStart);
                const footnoteMap = parseFootnotes(footnoteLines);

                /** @type {Array<{id:string,text:string,level:1|2}>} */
                const tocItems = [];
                /** @type {Record<string, {content: string}>} */
                const quoteData = {};
                let quoteIndex = 0;
                let sectionIndex = 0;
                let subIndex = 0;

                function pushToc(id, text, level) {
                    tocItems.push({ id, text, level });
                }

                function cleanTocText(raw) {
                    return (raw || '')
                        .replace(/<\s*sup[^>]*>[\s\S]*?<\s*\/\s*sup\s*>/gi, '')
                        .replace(/<\s*sub[^>]*>[\s\S]*?<\s*\/\s*sub\s*>/gi, '')
                        .replace(/\*\*(.*?)\*\*/g, '$1')
                        .replace(/\*(.*?)\*/g, '$1')
                        .replace(/^\*\s*/g, '')
                        .replace(/<[^>]+>/g, '')
                        .replace(/\s+/g, ' ')
                        .trim();
                }

                function makeId(level) {
                    if (level === 1) return `section-${sectionIndex}`;
                    return `section-${sectionIndex}-${subIndex}`;
                }

                let i = 0;

                // 标题
                let titleText = '坎瑞亚';
                if ((contentLines[i] || '').startsWith('# ')) {
                    titleText = (contentLines[i] || '').slice(2).trim();
                    i++;
                }
                pageTitleEl.innerHTML = injectFootnoteTooltips(normalizeInline(titleText), footnoteMap);

                // Intro：直到第一个二级标题
                const introParas = [];
                while (i < contentLines.length && !(contentLines[i] || '').startsWith('## ')) {
                    const line = (contentLines[i] || '').trimEnd();
                    if (line.trim()) introParas.push(line);
                    i++;
                }
                introEl.innerHTML = introParas.map((p, idx) => {
                    const raw = p.trim();
                    const isLatin = /^[a-zA-Z\s]+$/.test(raw) && raw.length <= 60;
                    let html = normalizeInline(raw);
                    html = injectFootnoteTooltips(html, footnoteMap);
                    if (isLatin && idx === 0) {
                        return `<p><strong class="khaenriah-font">${html}</strong></p>`;
                    }
                    return `<p>${html}</p>`;
                }).join('');

                // Timeline 构建
                timelineEl.innerHTML = '';
                let currentSection = null;
                let currentItem = null;

                function ensureSection() {
                    if (!currentSection) {
                        currentSection = document.createElement('div');
                        currentSection.className = 'timeline-section';
                        timelineEl.appendChild(currentSection);
                    }
                    return currentSection;
                }

                function startNewItem() {
                    const item = document.createElement('div');
                    item.className = 'timeline-item';
                    ensureSection().appendChild(item);
                    currentItem = item;
                    return item;
                }

                function appendToCurrent(targetEl) {
                    if (currentItem) currentItem.appendChild(targetEl);
                    else ensureSection().appendChild(targetEl);
                }

                function renderContentPara(text) {
                    const p = document.createElement('p');
                    p.className = 'timeline-content';
                    let html = normalizeInline(text);
                    html = injectFootnoteTooltips(html, footnoteMap);
                    p.innerHTML = html;
                    return p;
                }

                function renderSubtitle(text, tag = 'h3') {
                    const h = document.createElement(tag);
                    h.className = 'timeline-subtitle';
                    let html = normalizeInline(text);
                    html = injectFootnoteTooltips(html, footnoteMap);
                    h.innerHTML = html;
                    return h;
                }

                function renderQuoteBlock(quoteLines) {
                    quoteIndex += 1;
                    const quoteId = `quote-liyue-${quoteIndex}`;

                    const cleaned = quoteLines
                        .map(l => l.replace(/^>\s?/, '').replace(/\s{2,}$/, '').trimEnd())
                        .filter(l => l.trim().length > 0);

                    const first = cleaned[0] || '（引文）';
                    const preview = first.length > 70 ? (first.slice(0, 70) + '…') : first;

                    const block = document.createElement('div');
                    block.className = 'quote-block';
                    block.setAttribute('data-quote', quoteId);
                    block.innerHTML = `
                        <p class="quote-preview">${injectFootnoteTooltips(normalizeInline(preview), footnoteMap)}</p>
                        <p class="quote-link">[点击查看完整原文]</p>
                    `;

                    const fullHtml = cleaned.map(line => {
                        let html = normalizeInline(line);
                        html = injectFootnoteTooltips(html, footnoteMap);
                        return `<p>${html}</p>`;
                    }).join('');
                    quoteData[quoteId] = { content: fullHtml };

                    block.addEventListener('click', () => openModal(quoteData[quoteId].content));
                    return block;
                }

                function renderImage(imgLine) {
                    const m = imgLine.match(/image(\d+)\.png/i);
                    if (!m) return null;
                    const file = `image${m[1]}.png`;

                    const wrapper = document.createElement('div');
                    wrapper.className = 'timeline-image';

                    const img = document.createElement('img');
                    img.src = `../../img/context/Khaenriah/${file}`; //图片路径
                    img.alt = file;
                    img.style.maxWidth = 'calc(100% - 100px)';
                    img.style.display = 'block';
                    img.style.margin = '0 auto';
                    wrapper.appendChild(img);

                    return wrapper;
                }

                function isTableLine(line) {
                    const t = (line || '').trim();
                    return t.startsWith('|') && t.includes('|');
                }

                while (i < contentLines.length) {
                    const rawLine = contentLines[i] || '';
                    const line = rawLine.trimEnd();
                    const trimmed = line.trim();

                    // 空行：不输出，但用于分隔
                    if (!trimmed) { i++; continue; }

                    // 二级标题：新章节
                    if (trimmed.startsWith('## ')) {
                        sectionIndex += 1;
                        subIndex = 0;
                        currentItem = null;

                        const text = trimmed.slice(3).trim().replace(/^\*\s*/g, '');
                        if (!text) { i++; continue; }
                        const id = makeId(1);

                        currentSection = document.createElement('div');
                        currentSection.className = 'timeline-section';

                        const h2 = document.createElement('h2');
                        h2.className = 'timeline-period';
                        h2.id = id;
                        h2.innerHTML = injectFootnoteTooltips(normalizeInline(text), footnoteMap);
                        currentSection.appendChild(h2);
                        timelineEl.appendChild(currentSection);

                        pushToc(id, cleanTocText(text), 1);
                        i++;
                        continue;
                    }

                    // 三级标题：主标题
                    if (trimmed.startsWith('### ')) {
                        subIndex += 1;
                        currentItem = null;

                        const text = trimmed.slice(4).trim().replace(/^\*\s*/g, '');
                        if (!text) { i++; continue; }
                        const id = makeId(2);

                        const h4 = document.createElement('h4');
                        h4.className = 'timeline-main-title';
                        h4.id = id;
                        h4.innerHTML = injectFootnoteTooltips(normalizeInline(text), footnoteMap);
                        ensureSection().appendChild(h4);

                        pushToc(id, cleanTocText(text), 2);
                        i++;
                        continue;
                    }

                    // 四级标题：细分段落
                    if (trimmed.startsWith('#### ')) {
                        const raw = trimmed.slice(5).trim();

                        // 其它四级标题按子标题处理
                        currentItem = startNewItem();
                        currentItem.appendChild(renderSubtitle(raw, 'h5'));
                        i++;
                        continue;
                    }

                    // 引文（blockquote）
                    if (trimmed.startsWith('>')) {
                        const q = [];
                        while (i < contentLines.length && ((contentLines[i] || '').trim().startsWith('>'))) {
                            q.push(contentLines[i] || '');
                            i++;
                        }
                        appendToCurrent(renderQuoteBlock(q));
                        continue;
                    }

                    // 图片
                    if (trimmed.includes('[Image]') || trimmed.includes('![Image]')) {
                        const img = renderImage(trimmed);
                        if (img) {
                            appendToCurrent(img);
                            // 尝试读取下一行作为图注
                            let j = i + 1;
                            while (j < contentLines.length && !(contentLines[j] || '').trim()) j++;
                            const cap = (contentLines[j] || '').trim();
                            if (cap) {
                                const p = document.createElement('p');
                                p.className = 'image-caption';
                                p.innerHTML = injectFootnoteTooltips(normalizeInline(cap), footnoteMap);
                                img.appendChild(p);
                                i = j + 1;
                                continue;
                            }
                        }
                        i++;
                        continue;
                    }

                    // 表格
                    if (isTableLine(trimmed)) {
                        const block = [];
                        while (i < contentLines.length && isTableLine(contentLines[i] || '')) {
                            block.push(contentLines[i] || '');
                            i++;
                        }
                        const tableEl = renderTable(block, footnoteMap);
                        if (tableEl) appendToCurrent(tableEl);
                        continue;
                    }

                    // 项目符号（·）作为子标题
                    if (/^·/.test(trimmed)) {
                        currentItem = startNewItem();
                        currentItem.appendChild(renderSubtitle(trimmed, 'h3'));
                        i++;
                        continue;
                    }

                    // 常规文本：放入当前 item，否则新建 item
                    if (!currentItem) startNewItem();
                    currentItem.appendChild(renderContentPara(trimmed));
                    i++;
                }

                return { tocItems, quoteData };
            }

            function buildToc(tocItems) {
                tocList.innerHTML = '';

                tocItems.forEach(item => {
                    const li = document.createElement('li');
                    li.className = `toc-item level-${item.level}`;

                    const a = document.createElement('a');
                    a.href = '#' + item.id;
                    a.className = 'toc-link level-' + item.level;
                    a.textContent = item.text;

                    a.addEventListener('click', (e) => {
                        e.preventDefault();
                        const target = document.getElementById(item.id);
                        if (!target) return;
                        const offset = 100;
                        const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - offset;
                        window.scrollTo({ top: targetPosition, behavior: 'smooth' });
                        updateTocHighlight(item.id);
                    });

                    li.appendChild(a);
                    tocList.appendChild(li);
                });

                updateTocHighlight();
                window.addEventListener('scroll', () => updateTocHighlight());

                function updateTocHighlight(forceId = null) {
                    const links = document.querySelectorAll('.toc-link');
                    if (forceId) {
                        links.forEach(link => {
                            link.classList.toggle('active', link.getAttribute('href') === '#' + forceId);
                        });
                        return;
                    }

                    const sections = tocItems.map(item => document.getElementById(item.id)).filter(Boolean);
                    let current = '';
                    let minDistance = Infinity;

                    sections.forEach(section => {
                        const rect = section.getBoundingClientRect();
                        const distance = Math.abs(rect.top - 100);
                        if (rect.top <= 100 && rect.bottom >= 0) {
                            if (distance < minDistance) {
                                minDistance = distance;
                                current = section.id;
                            }
                        }
                    });

                    if (!current && sections.length > 0) current = sections[0].id;
                    links.forEach(link => {
                        link.classList.toggle('active', link.getAttribute('href') === '#' + current);
                    });
                }
            }

            if (!md.trim()) {
                introEl.innerHTML = '<p>（内容加载中）</p>';
                return;
            }

            const { tocItems } = parseMarkdownToPage(md);
            buildToc(tocItems);
        });